package Slave is
   -----------------------------------------------
   -- looks like Ada has only one-line comments
   -----------------------------------------------
   function sayHello (X : String) return Boolean;
   --
   -- comment
   --
   function sayGoodBye (X : String) return Boolean;
end Slave;
